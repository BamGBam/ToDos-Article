const Header = () => {
    return (
      <header>
        <h1>todos</h1>
        <p>Items will persist in the browser local storage for header part</p>
        {/* <input type="text" value="submit"/> */}
      </header>
    );
  };
export default Header;
  